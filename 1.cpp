#include<bits/stdc++.h>
using namespace std;
int main(){
	int n;
	cin>>n;
	if (n>0) cout<<"n is a postive number";
	else
	if (n<0) cout<<"n is a negative number";
	else
	cout<<"n is a equal to 0";
	return 0;
}
